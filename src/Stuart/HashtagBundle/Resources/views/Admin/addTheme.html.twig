{% extends "StuartHashtagBundle:Admin:index.html.twig" %}

{% block main %}
<p></p>
<p>Add a theme name and the associated CSS (up to 5000 chars)</p>
{{ form(form) }}

<h3>Helpful CSS Hints</h3>
<pre class="prettyprint linenums">
/* body background color */
body{ 
  background-color: #efefef;
  box-shadow: inset 0 0 100px rgba(50,50,50,.3);
  font-family: "Palatino Linotype", "Book Antiqua", Palatino, serif;
}
/* hashtag color */
#brand{ color: #888; font-family: "Palatino Linotype", "Book Antiqua", Palatino, serif; }

/* navbar color */
.navbar-inverse{ background-color: transparent }

/* navbar border color */
.navbar-inverse{ border-bottom:none }

/* navbar controls active */
.navbar-inverse .navbar-nav > .active > a, 
.navbar-inverse .navbar-nav > .active > a:hover, 
.navbar-inverse .navbar-nav > .active > a:focus { background-color:transparent !important }

/* link colors */
a { color: #454 }

/* link hover colors */
a:hover { color: #000 }

/* tweet background color */
.well.tweet { background-color: transparent !important;}

/* tweet background border */
.well.tweet { border: none!important}

/* tweet text color */
.well.tweet { color: #555 !important}

/* tweet text size */
.well.tweet { font-size: .85em !important}

/* tweet time color */
.tweet_time { color: #666 !important}

/* tweet time size */
.tweet_time { font-size: .75em !important}

/* tweet author name color */
.tweet_author a { color: #666 !important}

/* tweet author name size */
.tweet_author a { font-size: .75em !important}
</pre>
{% endblock %}